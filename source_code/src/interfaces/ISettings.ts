export interface ISettings {
  fontSize: number;
  leading: number;
  width: number;
  height: number;
  showingAscii: Boolean;
  invert: Boolean;
  background: String;
  color: String;
  luminance: Boolean;
  saturate: Boolean;
  threshold: number;
}